services:
  - type: web
    name: remote-mcp-server-render
    runtime: node
    rootDir: render
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
